# Программа конвертации температур

## Описание
Версия `0.1` программы `gradus` для конвертации температур между различными шкалами.
<details>
<summary>Cпойлер</summary>
Not a pet project. It's a lol project version v0.01.
</details>

## Поддерживаемые температурные шкалы

- **C** - Цельсий (Celsius)
- **F** - Фаренгейт (Fahrenheit)  
- **K** - Кельвин (Kelvin)
- **R** - Ранкин (Rankine)
- **Re** - Реомюр (Reaumur)
- **De** - Делиль (Delisle)
- **N** - Ньютон (Newton)
- **Ro** - Рёмер (Romer)

## Возможности

### 🔢 Работа с массивами значений
- **Ключ:** `-a` или `--array`
- **Описание:** Обработка массивов температурных значений
- **Пример:** `./gradus -a -s C -t F "0 20 40 60 80 100"`

### 🔧 Настраиваемые сепараторы
- **Ключи:** `--in-sep <separator>` и `--out-sep <separator>`
- **Описание:** Настройка сепараторов для входных и выходных данных
- **Примеры:** 
  - CSV: `--in-sep "," --out-sep ","`
  - Табуляция: `--in-sep "\t" --out-sep ","`
  - Точка с запятой: `--in-sep ";" --out-sep ";"`

### 📊 Табличный вывод
- **Ключ:** `-T` или `--table`
- **Описание:** Красиво форматированная таблица температур
- **Пример:** `./gradus -T`

### 📈 Графический вывод
- **Ключ:** `-G` или `--graph`
- **Описание:** ASCII-график сравнения температурных шкал
- **Пример:** `./gradus -G`

## Описание сборки

### [BUILD.md](BUILD.md)

## Использование

### Вывод таблицы температур:
```bash
./gradus -T
```

### Конвертация температур:
```bash
./gradus -s <source_scale> -t <target_scale> [temperature ...]
```

### 🔢 Работа с массивами значений

    Ключ: -a или --array
    Описание: Обработка массивов температурных значений
    Пример: ./gradus -a -s C -t F "0 20 40 60 80 100"

### 🔧 Настраиваемые сепараторы

    Ключи: --in-sep <separator> и --out-sep <separator>
    Описание: Настройка сепараторов для входных и выходных данных
    Примеры:
        CSV: --in-sep "," --out-sep ","
        Табуляция: --in-sep "\t" --out-sep ","
        Точка с запятой: --in-sep ";" --out-sep ";"

### 📊 Табличный вывод

    Ключ: -T или --table
    Описание: Красиво форматированная таблица температур
    Пример: ./gradus -T

### 📈 Графический вывод

    Ключ: -G или --graph
    Описание: ASCII-график сравнения температурных шкал
    Пример: ./gradus -G

### 📋 Полный список опций

    Usage: gradus [OPTIONS] -s <source_scale> -t <target_scale> [temperature ...]

    Options:
        -s, --source <scale>      Source temperature scale
        -t, --target <scale>      Target temperature scale
        -T, --table              Display temperature conversion table
        -G, --graph              Display temperature conversiongraph
        -a, --array              Enable array processing mode
        --in-sep <separator>      Input separator (default: whitespace)
        --out-sep <separator>     Output separator (default: newline)
        -h, --help               Display this help message

    Available scales: C (Celsius), F (Fahrenheit), K (Kelvin), R (Rankine), Re (Reaumur), De (Delisle), N (Newton), Ro (Romer)

### Одиночные значения
```bash
./gradus -s C -t F 25   # 77.00
./gradus -s F -t C 77   # 25.00
```

### Несколько значений
```bash
./gradus -s C -t F 0 20 40 60 100  
# 32.00 68.00 104.00 140.00 212.00
```

### Массив через пробел
```bash
./gradus -a -s C -t F "0 20 40 60 80 100"
```

### Массив с запятой
```bash
./gradus -a -s C -t F --in-sep "," --out-sep "," "0,20,40,60,80,100"
```

### Из файла
```bash
cat temps.txt | ./gradus -a -s C -t F --in-sep ","
```

### Обычный режим
```bash
echo "25" | ./gradus -s C -t F
```

### Режим массива
```bash
echo "20,25,30" | ./gradus -a -s C -t F --in-sep ","
```

### Таблица температур
```bash
./gradus -T
```

### График температур
```bash
./gradus -G
```

### Обработка серии измерений
```bash
echo "293.15 294.15 295.15" | ./gradus -a -s K -t C --out-sep ","
```

### Генерация данных для графика
```bash
for i in {0..100..5}; do echo -n "$i "; done | ./gradus -a -s C -t K --out-sep ","
```

### Диапазон рабочих температур
```
./gradus -a -s C -t F --in-sep "," --out-sep ";" "-40,-20,0,20,40,60,80,100,120"
```

### Конвертация спецификаций
```bash
./gradus -a -s F -t C "32 68 104 140 176 212"
```
 
### Температурные точки воды
```bash
echo "Температурные точки воды:" && ./gradus -a -s C -t F,K,R "0 100"
```

### Сравнение шкал
```bash
./gradus -T | grep "0.0\|100.0"
```

### 🛠 Компиляция 
```bash
    gcc -Wall -Wextra -o gradus gradus_enhanced.c -lm
```
### Физические ограничения

- Температура в шкалах Кельвин и Ранкин не может быть отрицательной (абсолютный ноль)
- Программа проверяет и сообщает об ошибках при нарушении этих ограничений